<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.jiyiji">
    <!-- 授予该程序使用摄像头的权限 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_firstji90"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".News.NewsActivity"></activity>
        <activity android:name=".BarChart.BarChartActivity" />
        <activity android:name=".PieChart.PieAndLineActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PieChart.PeiChartActivity" />
        <activity android:name=".AddAccount.AddBill" />
        <activity android:name=".AddAccount.ShowDetails" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Camera.CameraActivity" />
        <activity android:name=".Message.MessageActivity" />
        <activity android:name=".Explain.ExplainActivity" />
        <activity android:name=".Share.ShareActivity" />
        <activity android:name=".Feedback.FeedBackActivity" />
        <activity android:name=".Setting.SettingActivity" />
        <activity android:name=".Chart.ViewPagerActivity" />
        <activity android:name=".AddAccount.BookManager" />
    </application>

</manifest>